version: "3"

includes:
  infra:
    taskfile: infra/Taskfile.yml
    dir: infra

  proto:
    taskfile: proto/Taskfile.yml
    dir: proto

  core-service:code:
    taskfile: services/core/code/Taskfile.yml
    dir: services/core/code

  console-web:code:
    taskfile: apps/console-web/code/frontend/Taskfile.yml
    dir: apps/console-web/code/frontend

# noinspection YAMLSchemaValidation
.workflowSources: &workflowSources
  - ".github/workflows/*.yml"
  - ".github/actions/*/action.yml"

tasks:
  github-ci:lint:
    desc: Lint GitHub workflows
    sources: *workflowSources
    cmds:
      - github-ci lint

  github-ci:fixLint:
    desc: Fix lint issues in GitHub workflows
    sources: *workflowSources
    cmds:
      - github-ci lint --fix

  github-ci:upgrade:
    desc: Upgrade GitHub workflow actions to the latest versions
    cmds:
      - github-ci upgrade

  all:checkFormatting:
    desc: Check formatting for the entire codebase
    deps:
      - github-ci:lint
      - infra:checkFormatting
      - proto:checkFormatting
      - core-service:code:checkFormatting
      - console-web:code:checkFormatting

  all:fixFormatting:
    desc: Fix formatting for the entire codebase
    deps:
      - github-ci:fixLint
      - infra:fixFormatting
      - proto:fixFormatting
      - core-service:code:fixFormatting
      - console-web:code:fixFormatting

  all:lint:
    desc: Lint the entire codebase
    deps:
      - github-ci:lint
      - infra:lint
      - proto:lint
      - core-service:code:lint
      - console-web:code:lint

  all:analyze:
    desc: Analyze the entire codebase
    deps:
      - core-service:code:compile
      - console-web:code:typecheck
